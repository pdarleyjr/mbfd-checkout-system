name: Health Check

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  health-check:
    runs-on: ubuntu-latest
    steps:
      - name: Check Worker Health
        run: |
          response=\$(curl -s https://usar-ics212.pdarleyjr.workers.dev/health)
          echo "Worker response: \$response"
      
      - name: Check Frontend
        run: |
          response=\$(curl -s https://usar-ics212.pages.dev)
          echo "Frontend response received"
